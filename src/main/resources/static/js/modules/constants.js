export const CONTEXT_ROOT = document.querySelector('meta[name="_context_root"]').getAttribute('content');
export const CSRF_PARAMETER = document.querySelector('meta[name="_csrf_parameter"]').getAttribute('content');
export const CSRF_HEADER = document.querySelector('meta[name="_csrf_header"]').getAttribute('content');
export const CSRF_TOKEN = document.querySelector('meta[name="_csrf"]').getAttribute('content');
